# analyst_toolkit MCP Server dependencies
# Install with: pip install -r requirements-mcp.txt

# Server framework
fastapi>=0.111
uvicorn[standard]>=0.29
mcp>=1.2.1

# Data stack (matches core toolkit requirements)
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
PyYAML>=6.0
thefuzz>=0.20
XlsxWriter>=3.0
openpyxl>=3.1
pyarrow>=14.0

# Cloud I/O
google-cloud-storage>=2.14
gcsfs>=2024.2.0

# Pydantic (FastAPI dep, pinned for stability)
pydantic>=2.0

# analyst_toolkit_deploy — installed from vendor/ wheel in Dockerfile.mcp
# To update: bump version in analyst_toolkit_deployment_utility, build wheel,
# copy to vendor/, and update the pip install line in Dockerfile.mcp
